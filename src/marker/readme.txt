Marker to label the class of pixels.

Marker load original images and let user label classes of pixles. The marked images are then save into bitmap file.

source files: marker.cpp, CMakeLists.txt, number.png

requirement: OpenCV2.4.5, Cmake2.8

by Wasit Limprasert		
